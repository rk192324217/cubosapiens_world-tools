{
  "packageManager": "pnpm@8.0.0",
  "name": "cubosapiens",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "next dev",
    "build": "turbo run build",
    "deploy": "turbo run deploy"
  },
  "devDependencies": {
    "turbo": "latest",
    "typescript": "^5.0.0"
  }
}